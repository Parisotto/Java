package net.parisotto.uri;
import java.io.IOException;
import java.util.Scanner; 

/**
 * Extremamente Básico | Iniciante | Nível 5
 * URI Online Judge | URI 1001 | Sequencial
 * Adaptado por Neilor Tonin, URI  Brasil
 * FONTE: www.urionlinejudge.com.br
 * 
 * Leia 2 valores inteiros e armazene-os nas variáveis A e B. 
 * Efetue a soma de A e B atribuindo o seu resultado na variável X. 
 * Imprima X conforme exemplo apresentado abaixo.
 * 
 * Entrada: A entrada contém 2 valores inteiros.
 * Saída: Imprima a mensagem "X = " seguido pelo valor da variável X.
 * 
 * Exemplos de Entrada: 10 9	
 * Exemplo de Saída: X = 19
 */
 
public class Main {
    public static void main(String[] args) throws IOException {
        int A, B, X;
        
        Scanner scan = new Scanner(System.in);
        A = scan.nextInt();
        B = scan.nextInt();
        X = A + B;
        
        System.out.println("X = " + X);
    }
}
